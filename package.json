{
  "scripts": {
    "dev": "bun run --hot src/index.ts",
    "db:generate": "drizzle-kit generate:pg --schema=./src/db/schemas/index.ts",
    "db:migrate": "bun run src/db/migrate.ts",
    "db:seed": "bun run src/db/seed.ts"
  },
  "dependencies": {
    "@faker-js/faker": "^8.4.1",
    "drizzle-orm": "^0.29.4",
    "hono": "^4.0.9",
    "postgres": "^3.4.3"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "drizzle-kit": "^0.20.14"
  }
}
